class Solution {
    public int missingNumber(int[] nums) {
        int len=nums.length ;
        int i,sum1,miss,sum2=0;
        sum1=(len*(len+1))/2;
        for(i=0;i<len;i++){
         sum2 += nums[i]; 
        }
        miss=sum1-sum2;
        return miss;
    }
}
